version: '3.8'

# Connectors Platform - Development Environment
# AI Agent Integration Platform with 500+ connectors

services:
  # ============================================
  # Core Gateway Service
  # ============================================
  gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
      target: development
    container_name: connectors-gateway
    ports:
      - "3000:3000"
    environment:
      # Node Environment
      - NODE_ENV=development
      - LOG_LEVEL=debug

      # Service Dependencies
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=connectors-dev-2024
      - REDIS_URL=redis://redis:6379

      # Gateway Configuration
      - PORT=3000
      - MAX_TOOLS_PER_QUERY=5
      - CATEGORY_THRESHOLD=0.7
      - TOKEN_BUDGET_DEFAULT=2000
      - ENABLE_GRAPHRAG=true
      - ENABLE_PROGRESSIVE_LOADING=true

      # FAISS Configuration
      - FAISS_INDEX_PATH=/app/data/faiss-index
      - FAISS_EMBEDDING_DIM=1536

      # Cache Configuration
      - CACHE_TTL=3600
      - CACHE_MAX_SIZE=1000
    volumes:
      # Source code hot-reload
      - ./gateway/src:/app/src:ro
      - ./gateway/tests:/app/tests:ro
      - ./gateway/package.json:/app/package.json:ro
      - ./gateway/tsconfig.json:/app/tsconfig.json:ro

      # Data persistence
      - gateway-data:/app/data
      - gateway-logs:/app/logs

      # Node modules cache
      - gateway-node-modules:/app/node_modules
    depends_on:
      vault:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      - connectors-network

  # ============================================
  # HashiCorp Vault - Secrets Management
  # ============================================
  vault:
    image: hashicorp/vault:1.15
    container_name: connectors-vault
    cap_add:
      - IPC_LOCK
    ports:
      - "8200:8200"
    environment:
      # Development mode configuration
      - VAULT_DEV_ROOT_TOKEN_ID=dev-root-token
      - VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200
      - VAULT_LOG_LEVEL=info

      # Vault configuration
      - VAULT_ADDR=http://0.0.0.0:8200
    volumes:
      # Vault data persistence
      - vault-data:/vault/data
      - vault-logs:/vault/logs

      # Vault configuration (for production setup)
      - ./vault/config:/vault/config:ro
    command: server -dev -dev-root-token-id=dev-root-token
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8200/v1/sys/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - connectors-network

  # ============================================
  # Neo4j - GraphRAG Database
  # ============================================
  neo4j:
    image: neo4j:5.13-community
    container_name: connectors-neo4j
    ports:
      - "7474:7474"  # HTTP
      - "7687:7687"  # Bolt
    environment:
      # Authentication
      - NEO4J_AUTH=neo4j/connectors-dev-2024

      # Memory Configuration
      - NEO4J_server_memory_heap_initial__size=512m
      - NEO4J_server_memory_heap_max__size=2g
      - NEO4J_server_memory_pagecache_size=1g

      # Performance
      - NEO4J_dbms_memory_transaction_total_max=1g
      - NEO4J_dbms_checkpoint_interval_time=30s

      # APOC Plugin
      - NEO4J_PLUGINS=["apoc", "graph-data-science"]
      - NEO4J_dbms_security_procedures_unrestricted=apoc.*,gds.*
      - NEO4J_dbms_security_procedures_allowlist=apoc.*,gds.*
    volumes:
      # Data persistence
      - neo4j-data:/data
      - neo4j-logs:/logs
      - neo4j-import:/var/lib/neo4j/import
      - neo4j-plugins:/plugins
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:7474"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
    restart: unless-stopped
    networks:
      - connectors-network

  # ============================================
  # Redis - Caching Layer
  # ============================================
  redis:
    image: redis:7.2-alpine
    container_name: connectors-redis
    ports:
      - "6379:6379"
    command: >
      redis-server
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --save 60 1000
      --appendonly yes
      --appendfsync everysec
    volumes:
      # Data persistence
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - connectors-network

  # ============================================
  # MCP Server - GitHub Integration
  # ============================================
  mcp-github:
    build:
      context: ./integrations/code/github-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-github
    ports:
      - "3110:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=github
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
      - GITHUB_REDIRECT_URI=http://localhost:3110/oauth/callback
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/code/github-unified/src:/app/src:ro
      - ./integrations/code/github-unified/package.json:/app/package.json:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - github

  # ============================================
  # MCP Server - LinkedIn Integration
  # ============================================
  mcp-linkedin:
    build:
      context: ./integrations/communication/linkedin-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-linkedin
    ports:
      - "3120:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=linkedin
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
    volumes:
      - ./integrations/communication/linkedin-unified/src:/app/src:ro
      - ./integrations/communication/linkedin-unified/package.json:/app/package.json:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - linkedin

  # ============================================
  # MCP Server - Reddit Integration
  # ============================================
  mcp-reddit:
    build:
      context: ./integrations/communication/reddit-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-reddit
    ports:
      - "3200:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=reddit
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
    volumes:
      - ./integrations/communication/reddit-unified/src:/app/src:ro
      - ./integrations/communication/reddit-unified/package.json:/app/package.json:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - reddit

  # ============================================
  # MCP Server - Notion Integration
  # ============================================
  mcp-notion:
    image: notionhq/notion-mcp-server:latest
    container_name: connectors-mcp-notion
    ports:
      - "3100:3000"
    environment:
      - NODE_ENV=production
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=info
    depends_on:
      gateway:
        condition: service_healthy
    networks:
      - connectors-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    profiles:
      - mcp-servers
      - notion

  # ============================================
  # MCP Server - Gmail Integration (Google Workspace Phase 1)
  # ============================================
  mcp-gmail:
    build:
      context: ./integrations/communication/gmail-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-gmail
    ports:
      - "3130:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=gmail
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/communication/gmail-unified/src:/app/src:ro
      - ./integrations/communication/gmail-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - gmail

  # ============================================
  # MCP Server - Google Calendar Integration (Google Workspace Phase 1)
  # ============================================
  mcp-calendar:
    build:
      context: ./integrations/productivity/calendar-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-calendar
    ports:
      - "3131:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=calendar
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/productivity/calendar-unified/src:/app/src:ro
      - ./integrations/productivity/calendar-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - calendar

  # ============================================
  # MCP Server - Google Drive Integration (Google Workspace Phase 1)
  # ============================================
  mcp-drive:
    build:
      context: ./integrations/storage/drive-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-drive
    ports:
      - "3132:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=drive
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/storage/drive-unified/src:/app/src:ro
      - ./integrations/storage/drive-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - drive

  # ============================================
  # MCP Server - Google Tasks Integration (Google Workspace Phase 2)
  # ============================================
  mcp-tasks:
    build:
      context: ./integrations/productivity/tasks-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-tasks
    ports:
      - "3137:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=tasks
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/productivity/tasks-unified/src:/app/src:ro
      - ./integrations/productivity/tasks-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - tasks

  # ============================================
  # MCP Server - Google Docs Integration (Google Workspace Phase 2)
  # ============================================
  mcp-docs:
    build:
      context: ./integrations/documents/docs-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-docs
    ports:
      - "3133:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=docs
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/documents/docs-unified/src:/app/src:ro
      - ./integrations/documents/docs-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - docs

  # ============================================
  # MCP Server - Google Sheets Integration (Google Workspace Phase 2)
  # ============================================
  mcp-sheets:
    build:
      context: ./integrations/documents/sheets-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-sheets
    ports:
      - "3134:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=sheets
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/documents/sheets-unified/src:/app/src:ro
      - ./integrations/documents/sheets-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - sheets

  # ============================================
  # MCP Server - Google Slides Integration (Google Workspace Phase 3)
  # ============================================
  mcp-slides:
    build:
      context: ./integrations/documents/slides-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-slides
    ports:
      - "3135:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=slides
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/documents/slides-unified/src:/app/src:ro
      - ./integrations/documents/slides-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - slides

  # ============================================
  # MCP Server - Google Forms Integration (Google Workspace Phase 3)
  # ============================================
  mcp-forms:
    build:
      context: ./integrations/productivity/forms-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-forms
    ports:
      - "3136:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=forms
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/productivity/forms-unified/src:/app/src:ro
      - ./integrations/productivity/forms-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - forms

  # ============================================
  # MCP Server - Google Chat Integration (Google Workspace Phase 3)
  # ============================================
  mcp-chat:
    build:
      context: ./integrations/communication/chat-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-chat
    ports:
      - "3138:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=chat
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/communication/chat-unified/src:/app/src:ro
      - ./integrations/communication/chat-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - chat

  # ============================================
  # MCP Server - Google Custom Search Integration (Google Workspace Phase 3)
  # ============================================
  mcp-search:
    build:
      context: ./integrations/search/google-search-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-search
    ports:
      - "3139:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=search
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - GOOGLE_PSE_API_KEY=${GOOGLE_PSE_API_KEY:-}
      - GOOGLE_PSE_ENGINE_ID=${GOOGLE_PSE_ENGINE_ID:-}
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/search/google-search-unified/src:/app/src:ro
      - ./integrations/search/google-search-unified/package.json:/app/package.json:ro
      - ./integrations/shared:/app/shared:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - google-workspace
      - search

  # ============================================
  # MCP Server - Product Hunt Integration
  # ============================================
  mcp-producthunt:
    build:
      context: ./integrations/productivity/producthunt-unified
      dockerfile: Dockerfile
    container_name: connectors-mcp-producthunt
    ports:
      - "3140:3000"
    environment:
      - NODE_ENV=development
      - GATEWAY_URL=http://gateway:3000
      - LOG_LEVEL=debug
      - INTEGRATION=producthunt
      - VAULT_ADDR=http://vault:8200
      - VAULT_TOKEN=dev-root-token
      - PORT=3000
    volumes:
      # Source code hot-reload
      - ./integrations/productivity/producthunt-unified/src:/app/src:ro
      - ./integrations/productivity/producthunt-unified/package.json:/app/package.json:ro
    depends_on:
      gateway:
        condition: service_healthy
      vault:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - mcp-servers
      - producthunt

  # ============================================
  # Monitoring - Prometheus (Optional)
  # ============================================
  prometheus:
    image: prom/prometheus:v2.47.0
    container_name: connectors-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - monitoring

  # ============================================
  # Monitoring - Grafana (Optional)
  # ============================================
  grafana:
    image: grafana/grafana:10.1.0
    container_name: connectors-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources:ro
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    restart: unless-stopped
    networks:
      - connectors-network
    profiles:
      - monitoring

# ============================================
# Named Volumes
# ============================================
volumes:
  gateway-data:
    driver: local
  gateway-logs:
    driver: local
  gateway-node-modules:
    driver: local
  vault-data:
    driver: local
  vault-logs:
    driver: local
  neo4j-data:
    driver: local
  neo4j-logs:
    driver: local
  neo4j-import:
    driver: local
  neo4j-plugins:
    driver: local
  redis-data:
    driver: local
  prometheus-data:
    driver: local
  grafana-data:
    driver: local

# ============================================
# Networks
# ============================================
networks:
  connectors-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16

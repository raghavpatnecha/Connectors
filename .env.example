# Connectors Platform - Environment Variables Template
# Copy this file to .env and fill in with secure values

# ============================================
# SECURITY NOTICE
# ============================================
# DO NOT commit .env files to version control
# Generate secure random values for all credentials
# Use: openssl rand -hex 16 (for tokens)
#      openssl rand -base64 24 (for passwords)

# ============================================
# Vault Configuration
# ============================================
# Root token for Vault (development only)
# Production: Use Vault's built-in authentication
VAULT_DEV_ROOT_TOKEN_ID=your-secure-vault-token-here-32-chars-minimum

# ============================================
# Neo4j Configuration
# ============================================
# Neo4j database password
# Must be at least 8 characters
NEO4J_PASSWORD=your-secure-neo4j-password-here-min-8-chars

# ============================================
# Redis Configuration
# ============================================
# Redis password (optional for development)
# REDIS_PASSWORD=your-secure-redis-password-here

# ============================================
# Gateway Configuration
# ============================================
# Node environment (development, staging, production)
NODE_ENV=development

# Log level (debug, info, warn, error)
LOG_LEVEL=debug

# ============================================
# Rate Limiting Configuration
# ============================================
# Global rate limit (requests per second)
RATE_LIMIT_GLOBAL_RPS=1000

# Per-tenant rate limit (requests per minute)
RATE_LIMIT_TENANT_RPS=100

# Anonymous request rate limit (requests per minute)
RATE_LIMIT_ANONYMOUS_RPS=10

# ============================================
# OAuth Client Credentials (Optional)
# ============================================
# GitHub OAuth (if using GitHub integration)
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# Google OAuth (if using Google Workspace integrations)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# API Keys (Optional)
# ============================================
# Custom search engine (Google Custom Search)
# GOOGLE_PSE_API_KEY=your-google-custom-search-api-key
# GOOGLE_PSE_ENGINE_ID=your-search-engine-id

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Generate secure credentials (see examples below)
# 3. Update .env with your values
# 4. Verify .env is in .gitignore

# Examples for generating secure credentials:
# - Vault token:  openssl rand -hex 16
# - Neo4j password: openssl rand -base64 24
# - Redis password: openssl rand -base64 24
